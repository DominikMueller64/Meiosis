#include <vector>

#ifndef CROSSOVER_H
#define CROSSOVER_H
std::vector<double> crossover(const double L,
                              const int m,
                              const double p,
                              const bool obligate_chiasma,
                              const double Lstar);
#endif
